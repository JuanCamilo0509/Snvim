#ifndef ${upper_name}_HPP
#define ${upper_name}_HPP

/**
 * @file ${filename}.${fileext}
 * @author ${gh-user} (${gh-email})
 * @date ${date}
 */

class ${filename} {
public:
    ${filename}();
    ~${filename}();
};

#endif // ${upper_name}_HPP

